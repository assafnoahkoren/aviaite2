import React from 'react';
import { Box, Text } from '@mantine/core';

const MessagesList: React.FC = () => {
  return (
    <Box p={16}>
      <Text>Messages List (chat history)</Text>
    </Box>
  );
};

export default MessagesList; 