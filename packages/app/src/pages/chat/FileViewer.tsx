import React from 'react';
import { Box, Text } from '@mantine/core';

const FileViewer: React.FC = () => {
  return (
    <Box p={16}>
      <Text>File Viewer (right panel)</Text>
    </Box>
  );
};

export default FileViewer; 